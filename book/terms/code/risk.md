# The `risk` in Base Link `code`

The risk statement, like unsafe in rust.

```
risk take
  save status
    call save-file
      wait take
      text <path.txt>
      text <content >
        loan z
        cord < more content>
```
