export function filterBlue(input, type) {
    const result = [];
    // const children = code.assumeChildren(input)
    // for (const node of children) {
    //   if (code.isBlue(node, type)) {
    //     result.push(node)
    //   }
    // }
    return result;
}
export function filterRed(input, name) {
    const result = [];
    // const children = code.assumeChildren(input)
    // for (const node of children) {
    //   if (code.isRed(node, Mesh.Gather) && node.name === name) {
    //     result.push(node)
    //   }
    // }
    return result;
}
export function findBlue(input, type) {
    // const children = code.assumeChildren(input)
    // for (const node of children) {
    //   if (code.isBlue(node, type)) {
    //     return node
    //   }
    // }
    return undefined;
}
export function findRed(input, name) {
    // const children = code.assumeChildren(input)
    // for (const node of children) {
    //   if (code.isRed(node, Mesh.Gather) && node.name === name) {
    //     return node
    //   }
    // }
    return undefined;
}
//# sourceMappingURL=placeholder.js.map