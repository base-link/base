export function filterBlue(load, type) {
    const result = [];
    // const children = code.assumeChildren(load)
    // for (const node of children) {
    //   if (code.isBlue(node, type)) {
    //     result.push(node)
    //   }
    // }
    return result;
}
export function filterRed(load, name) {
    const result = [];
    // const children = code.assumeChildren(load)
    // for (const node of children) {
    //   if (code.isRed(node, Mesh.Gather) && node.name === name) {
    //     result.push(node)
    //   }
    // }
    return result;
}
export function findBlue(load, type) {
    // const children = code.assumeChildren(load)
    // for (const node of children) {
    //   if (code.isBlue(node, type)) {
    //     return node
    //   }
    // }
    return undefined;
}
export function findRed(load, name) {
    // const children = code.assumeChildren(load)
    // for (const node of children) {
    //   if (code.isRed(node, Mesh.Gather) && node.name === name) {
    //     return node
    //   }
    // }
    return undefined;
}
//# sourceMappingURL=placeholder.js.map